# Naive Bayes, Feature Selection, and PCA

This project explores the performance of the Naive Bayes classifier on different types of datasets. It compares three approaches: using all original features (baseline), applying feature selection, and reducing dimensionality using Principal Component Analysis (PCA) implemented from scratch with NumPy. The goal is to analyze how feature selection and feature reduction affect classification accuracy and overall model performance.
